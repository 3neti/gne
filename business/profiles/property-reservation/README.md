# Property Reservation Profile

This canonical example proves that a human or AI can follow one reservation from brochure through certificate using repository evidence. It is intentionally narrow, uses synthetic data, and delegates payment settlement and document rendering to future adapters.

Accepted artifacts under `examples/artifacts/` are immutable. Corrections add a revision and an explicit supersession relationship.
